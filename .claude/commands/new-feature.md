在 `src/features/$ARGUMENTS/` 下创建一个新功能模块。

要求：
1. 至少创建 `components/` 和 `index.ts`
2. 需要状态时再创建 `hooks/`、`api/`、`store/`、`types/`
3. 页面组件使用 `export default`
4. 组件样式优先使用 CSS Modules
5. 更新 `src/app/routes.tsx` 暴露路由入口
6. 完成后运行 `pnpm lint`
