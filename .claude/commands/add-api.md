在 feature `$ARGUMENTS` 中新增 API 调用。

要求：
1. 请求与响应类型放在该 feature 的 `types/`
2. API 实现放在该 feature 的 `api/`
3. 供页面消费的封装逻辑放在该 feature 的 `hooks/`
4. 共享请求能力放在 `src/shared/lib/`
5. 更新该 feature 的 `index.ts`
6. 完成后运行 `pnpm lint`
