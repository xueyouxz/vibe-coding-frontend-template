# Code Style

- Imports order: external packages, internal aliases, local modules, then styles.
- Prefer explicit named constants over inline magic values when reused.
- Keep comments short and only where logic is non-obvious.
- Do not create catch-all `index.ts` files outside feature public APIs.
- Avoid large global utility buckets.
