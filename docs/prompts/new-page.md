## New Page Prompt

在 `{feature_name}` 功能模块下创建新页面 `{page_name}`。

页面目标：{goal}
交互需求：{interaction}
视觉风格：{style}

要求：
- 遵循 `src/features/<name>/` 结构
- 页面组件默认导出
- 样式优先使用 CSS Modules
- 必要时补充 `hooks/`、`types/`、`api/`
- 更新 `src/app/routes.tsx`
- 完成后运行 `pnpm lint`
